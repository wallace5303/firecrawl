# 安装

```bash
git clone git@github.com:wallace5303/firecrawl.git
cd firecrawl

git remote add upstream https://github.com/firecrawl/firecrawl.git

git fetch upstream
git checkout -b gsx/2.6.0 v2.6.0
docker compose build
docker compose up -d
```





